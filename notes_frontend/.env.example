# Nuxt Notes App - .env example
# Copy this file to `.env` and adjust values as needed.

# API base used when using the internal mock API (Nuxt server routes under /api)
# Usually leave as /api for local development.
NUXT_PUBLIC_API_BASE=/api

# External backend base URL (e.g., https://api.example.com)
# When provided AND the feature flag USE_EXTERNAL_BACKEND=true is set,
# the app will use this URL for API requests instead of /api.
NUXT_PUBLIC_BACKEND_URL=

# Public frontend URL (e.g., https://notes.example.com)
# Optional; useful for redirects or absolute links.
NUXT_PUBLIC_FRONTEND_URL=

# WebSocket server URL (if applicable), e.g., wss://ws.example.com
# Optional; not used by the current mock notes app but reserved for future use.
NUXT_PUBLIC_WS_URL=

# Node environment hint (development|staging|production)
# Optional; for tooling and diagnostics.
NUXT_PUBLIC_NODE_ENV=development

# Disable Next/Nuxt telemetry (string "1" or "true" disables)
# Optional.
NUXT_PUBLIC_NEXT_TELEMETRY_DISABLED=1

# Enable source maps in builds (true|false)
# Optional; helpful for debugging.
NUXT_PUBLIC_ENABLE_SOURCE_MAPS=false

# Port to run the dev/preview server on (may be overridden by Nuxt config)
# Optional; default dev port is 3000.
NUXT_PUBLIC_PORT=3000

# Trust proxy settings (true|false or a proxy count) for reverse proxy environments
# Optional.
NUXT_PUBLIC_TRUST_PROXY=false

# Log level for the app (debug|info|warn|error)
# Optional.
NUXT_PUBLIC_LOG_LEVEL=info

# Healthcheck path that returns a simple OK (if implemented)
# Optional; not used directly in this app but reserved for infra checks.
NUXT_PUBLIC_HEALTHCHECK_PATH=/healthz

# Feature flags control certain runtime behaviors.
# Supported format:
#   JSON example: {"USE_EXTERNAL_BACKEND": true}
#   CSV example:  USE_EXTERNAL_BACKEND=true,EXPERIMENT_X=false
# To switch to an external backend, set:
#   NUXT_PUBLIC_BACKEND_URL=https://api.example.com
#   NUXT_PUBLIC_FEATURE_FLAGS={"USE_EXTERNAL_BACKEND":true}
NUXT_PUBLIC_FEATURE_FLAGS={"USE_EXTERNAL_BACKEND":false}

# Experiments toggle (string as JSON or CSV)
# Optional; reserved for future experimental features.
NUXT_PUBLIC_EXPERIMENTS_ENABLED=false
